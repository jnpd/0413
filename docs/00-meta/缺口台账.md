# 缺口台账

## 文档信息

| 项 | 内容 |
|---|---|
| 最后更新 | 2026-04-13 |
| 维护人 | 待补 |

## 1. 目标

记录当前知识库与实现工作区中仍然缺失、但会直接影响交接、联调、开发或发布判断的关键信息。

## 2. P0 缺口

| 编号 | 领域 | 当前事实 | 缺口 |
|---|---|---|---|
| P0-01 | 外部需求来源 | 当前仅能读取 `README.md`、`REPO_WIKI.md`、`AGENTS.md`、`findings.md`、`progress.md` | 权威 PRD、设计文档、设备协议原文未进入工作区 |
| P0-02 | 规格真源 | 当前未发现可直接复用的 `openspec/` 或冻结的 change/contracts | 高风险规则目前主要依赖项目级文档，缺少 feature 级真源 |
| P0-03 | 前端工程 | 当前工作区已存在可验证的前端原型工程，能确认 `React + Vite + TypeScript + Tailwind` | Ant Design 正式后台实现、请求层规范、状态管理方案仍未冻结 |
| P0-04 | 责任人 | 当前仓库未发现 RACI 或值班责任说明 | 交接时无法直接定位产品、后端、测试、运维 owner |

## 3. P1 缺口

| 编号 | 领域 | 当前事实 | 缺口 |
|---|---|---|---|
| P1-01 | 多租户隔离 | `application.yml` 已开启 BladeX 多租户增强，`tenant_id` 已进入表结构 | 仓库模块代码中尚未看到显式 tenant 过滤与越权校验说明 |
| P1-02 | 权限边界 | `strict-token` / `strict-header` 已开启 | 页面权限、按钮权限、数据权限的项目级编码与菜单映射未补齐 |
| P1-03 | 删除守卫 | `AGENTS.md` 已明确删除守卫为高风险项 | `WarehouseServiceImpl#removeWarehouse` 当前仅按 ID 删除，守卫规则未落地 |
| P1-04 | 出库闭环 | `outboundWarehouse` 已更新 `stock_status` 与 `outbound_time` | 与包装、审计、重复出库阻断的完整规则仍缺失 |
| P1-05 | 导入规则 | 已有 `/warehouse/import` 接口壳 | 模板、校验、整单回滚/部分成功策略未落地 |
| P1-06 | 设备链路 | RabbitMQ 与 `tb_meter_latest` 已有配置与表结构 | 实际消费者、回执、超时处理实现未在当前工作区定位到 |

## 4. P2 缺口

| 编号 | 领域 | 当前事实 | 缺口 |
|---|---|---|---|
| P2-01 | 发布流程 | 当前已补项目级 `docs/06-release-ops/发布流程.md` 初稿 | 真实部署平台、审批流、回滚脚本仍待补 |
| P2-02 | 缺陷沉淀 | 当前已能整理 2026-04-09 的两个已知案例 | 历史线上缺陷库未回灌到当前仓库 |
| P2-03 | 文档维护机制 | `AGENTS.md` 与 `REPO_WIKI.md` 已要求知识回写 | 未发现固定 owner、更新周期和验收机制 |

## 5. 建议补齐顺序

1. 补权威 PRD / 设计源锚点
2. 补 feature 级规格真源
3. 冻结前端正式技术栈与后台 UI 方案
4. 补 RACI 与发布负责人
5. 补删除守卫、多租户、出库/导入等高风险规则的落地证据

## 6. 使用规则

- 只要缺口会影响实现或验收，就优先记录在这里。
- 未补齐的事项可以先在项目级文档中写“待补”，但不能伪造结论。
- 缺口关闭后，要同步更新对应知识页，不只改本台账。
