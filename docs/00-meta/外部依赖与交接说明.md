# 外部依赖与交接说明

## 文档信息

| 项 | 内容 |
|---|---|
| 用途 | 补齐当前知识库工作区无法自包含的信息，降低接手成本 |
| 最后更新 | 2026-04-13 |
| 维护人 | 待补 |

> 本文件只记录当前仓库已验证的交接事实与待补项。业务规则以项目级 `docs/` 正文为准，通用约束以 [通用契约索引.md](./通用契约索引.md) 为准。

### 关联文档

| 文档 | 关系 |
|---|---|
| `README.md` | 项目入口与导航 |
| `REPO_WIKI.md` | 知识地图与阅读顺序 |
| `AGENTS.md` | Agent 工作原则与高风险事项 |
| `docs/00-meta/缺口台账.md` | 当前缺失真源与待补项 |

## 1. 真实代码库与分支策略

### 1.1 已验证仓库信息

| 项 | 当前值 |
|---|---|
| Git 远程 URL | `https://github.com/jnpd/demo0409.git` |
| 当前分支 | `main` |
| 当前工作树状态 | 本地工作区尚未形成提交历史，`origin/main` 当前不可追踪 |
| 与本知识库目录的关系 | 单工作区，包含知识文档目录 `docs/`、后端目录 `bladex-boot/`、前端原型目录 `frontend/` |

### 1.2 当前仓库结构说明

- `docs/`：项目级知识与交接文档
- `bladex-boot/`：当前可直接验证的后端工程目录
- `frontend/`：前端原型工程目录，当前已可验证到 `React + Vite + TypeScript + Tailwind`，Ant Design 为后续目标
- `changes/`：change 入口目录，当前未发现可直接复用的规格正文

### 1.3 文档与实现对齐方式

- 当前仓库没有可直接复用的 `openspec/` 真源；如后续补齐，优先级应高于项目级总览文档。
- 已冻结或后续补齐的 change/contracts 应优先覆盖项目级 `docs/`。
- 当前项目级知识页优先级高于分散落盘的通用契约与外部 starter 模板。

## 2. 环境与配置锚点

### 2.1 本地可验证入口

| 项 | 当前事实 |
|---|---|
| 后端启动端口 | `bladex-boot/bladex-boot/src/main/resources/application.yml` 中配置为 `8010` |
| MySQL 配置位置 | `bladex-boot/bladex-boot/src/main/resources/application-dev.yml` |
| Redis 配置位置 | `bladex-boot/bladex-boot/src/main/resources/application-dev.yml` |
| RabbitMQ 配置位置 | `bladex-boot/bladex-boot/src/main/resources/application.yml` |
| 仓库 SQL 升级脚本 | `bladex-boot/bladex-boot/doc/sql/upgrade/` |

### 2.2 密钥与敏感信息约束

- 当前配置文件中存在数据库、Redis、RabbitMQ 等敏感连接信息。
- 知识库文档不复制明文账号、密码、外网地址；只记录配置所在路径与获取方式。
- 若后续要对外移交，需先补齐脱敏版环境获取流程。

### 2.3 环境搭建检查

- [ ] JDK / Maven 版本与运行方式待补
- [ ] 前端真实运行目录与 Node 版本待补
- [ ] MySQL、Redis、RabbitMQ 的脱敏接入说明待补
- [ ] 至少一条本地可复现实测 smoke 命令待补

## 3. PRD / 设计源与版本锚点

### 3.1 当前仓库内可直接读取的来源

| 来源 | 位置 | 说明 |
|---|---|---|
| 项目入口 | `README.md` | 当前项目目标、范围与导航 |
| 知识索引 | `REPO_WIKI.md` | 当前知识布局与路径口径 |
| Agent 规则 | `AGENTS.md` | 高风险事项与工作流 |
| 历史实施记录 | `findings.md`、`progress.md` | 2026-04 的实现与联调纪要 |
| 后端实现真源 | `bladex-boot/` | 当前可验证的代码、配置与 SQL |
| 前端原型真源 | `frontend/` | 当前可验证的原型工程与构建配置 |

### 3.2 当前缺失的外部锚点

| 项 | 当前状态 |
|---|---|
| 权威 PRD 原文 | 未在当前工作区找到 |
| 架构设计原文 | 未在当前工作区找到 |
| 设备协议原文 | 未在当前工作区找到 |
| 发布环境说明 | 未在当前工作区找到 |
| 责任人 / RACI | 未在当前工作区找到 |

### 3.3 版本冲突处理

优先级固定为：

```text
change/contracts 或后续补齐的 openspec
> 项目级 docs
> 分散落盘的通用契约
> 外部 starter 模板
```

## 4. 当前已验证的中间件与链路预留

| 项 | 当前事实 |
|---|---|
| MySQL | 后端采用 MySQL 驱动，仓库相关 SQL 已落 `water-dev.mysql.warehouse-*.sql` |
| Redis | 本地配置文件中已存在 Redis 配置 |
| RabbitMQ | 已存在 `queue_report`、`queue_dense_collect`、`exchange_arctech`、`send_cmd`、`send_cmd_timeout` 等配置 |
| 实时表 | 已存在 `tb_meter_latest` 建表与种子脚本，说明设备上报链路至少预留到了数据落表层 |

## 5. RACI 与交接责任

当前工作区未发现可直接复用的 RACI 文档，以下角色全部待补：

- 产品负责人
- 技术/架构负责人
- 后端负责人
- 前端负责人
- 测试负责人
- 发布/运维负责人
- 知识库维护人

## 6. 修订记录

| 日期 | 修订人 | 说明 |
|---|---|---|
| 2026-04-13 | Codex | 根据当前工作区实际结构、Git 信息、后端配置与 SQL 重新回填初稿 |
